user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 telegraph_bot.py"

env:
  - "ENVIRONMENT=production"
